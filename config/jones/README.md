# Jones demo

This event matches `tests/inputs/webcast_jones.zip`: one contest, Jones, and
one site, Geral, both including all teams. It has 20 teams, 8 problems, and
134 runs, with no external media or scoring overrides.

The root Compose file mounts this manifest and the committed development
configuration examples. See the [quick start](../../README.md).
