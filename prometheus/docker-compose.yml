# Generate from the repository root with `make configure-docker`.
# Start using `docker compose --profile monitoring up -d` here,
# or `make monitor-docker` from the root.
include:
  - ../.generated/compose.json
