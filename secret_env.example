# Copy this file to ./secret_env for a working local setup. Keep only private
# credentials here; public settings belong in .env.

# Webcast source used only by the feeder (a URL or local zip path).
BOCA_URL=tests/inputs/webcast_jones.zip

# Private internal API token. The token name and URL are in .env.
INTERNAL_TOKEN=development-token
