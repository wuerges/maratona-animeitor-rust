[package]
name = "maratona_animeitor_rust"
version = "0.1.0"
authors = ["Emilio Wuerges <wuerges@gmail.com>"]
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
quickcheck = "0.9"
quickcheck_macros = "0.9"

[workspace]
members = [
    "lib-server",
    "lib-seed",
    "turbineitor",
]