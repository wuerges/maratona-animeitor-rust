# Development fixture. For a real event, copy to event-secrets.toml (ignored)
# and replace this mapping with its event name and private webcast URL.
# Local paths are relative to this file.
[webcasts]
jones = "tests/inputs/webcast_jones.zip"
